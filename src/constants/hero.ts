/**
 * Hero copy for the v3 "Command Line" landing screen.
 */
export const HERO = {
  /** Terminal boot line above the headline. */
  prompt: "> SYSTEM READY.",
  /** Version chip beside the prompt. */
  version: "[VER_3.0]",
  /** Headline lines — each rendered on its own row. */
  headline: ["CRAFTING ORDER.", "FROM CHAOS.", "ONE LINE AT A TIME."],
  /** Supporting paragraph. */
  body: "Full-stack developer focused on scalable architecture, clean code, and performant interfaces. Translating complex requirements into elegant technical solutions.",
  /** Primary CTA — scrolls to the work gallery. */
  ctaWork: "> VIEW_WORK",
  /** Secondary CTA — opens the resume. */
  ctaResume: "$ cat resume.pdf",
};
